export default function OperatorHome() {
  return (
    <main style={{ maxWidth: 820, margin: '0 auto', padding: '40px 24px' }}>
      <h1 style={{ color: 'var(--teal)' }}>Operator Console</h1>
      <p style={{ color: 'var(--muted)' }}>
        P1–P3 will add: inventory management, menu &amp; daily caps, fleet &amp;
        driver onboarding, food order board, and the live dispatch board.
      </p>
      <a href="/" style={{ fontSize: 13 }}>← Back</a>
    </main>
  );
}
