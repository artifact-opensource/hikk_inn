export default function DriverHome() {
  return (
    <main style={{ maxWidth: 820, margin: '0 auto', padding: '40px 24px' }}>
      <h1 style={{ color: 'var(--teal)' }}>Driver Companion</h1>
      <p style={{ color: 'var(--muted)' }}>
        P2 will add: availability toggle, nearby ride requests, accept flow, and
        live GPS sharing. (Mobile-first / PWA in MVP.)
      </p>
      <a href="/" style={{ fontSize: 13 }}>← Back</a>
    </main>
  );
}
